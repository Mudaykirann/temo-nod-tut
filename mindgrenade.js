a = 10;
b = 20;

function add() {
    console.log(`The sum of ${a} and ${b} is ${a + b}`);
}

add(); //just function call

//Node wrap it's all functionalities  inside a function called module, which can be accessed by using "require" method in another file.